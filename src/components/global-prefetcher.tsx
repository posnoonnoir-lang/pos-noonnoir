"use client"

/**
 * GlobalPrefetcher — DISABLED to diagnose connection issues.
 * All pages self-cache on first visit, so prefetch is only a nice-to-have.
 * Re-enable once Supabase connection limits are resolved.
 */
export function GlobalPrefetcher() {
    return null
}
